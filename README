Java app for generating and viewing the Mandelbrot set, but mainly for me to learn some java!

- Image can be centred by clicking on point to centre around
- Zooming can be done with - and + keys and reset with =
- Slider at the bottom to control the secondary colour based on an integer representing the RGB spectrum
